<%= render '/layouts/toptiles' %>

    
    <div id="thumb-wrapper">
        <section id="work-detail-wrapper" class="swipe">
                
        </section> <!--section#work-detail-wrapper-->
        
        <section id="works" class="work-launcher">
            <ul id="thumbs">
              <% @user_sites.each do |user_site| %>
                <li class="thumb cat-identity"><a href="user_sites/<%= user_site.id %>" title=""><img src="/assets/sites/<%= user_site.site.icon %>" alt="work-thumb-01" /></a></li>
                <% end %>
                
            </ul>
  </section><!--end section#works-->
  </div><!--end div#thumbwrapper-->

  <%= render '/layouts/bottomtiles' %>



<!-- ACCOUNTS GO HERE
<% @user_sites.each do |user_site| %>
  <tr>
    <td><%= user_site.site.url %></td>
    <td><%= user_site.username %></td>
    <td><%= user_site.password %></td>
    <td><%= link_to 'Show', user_site %></td>
    <td><%= link_to 'Edit', edit_user_site_path(user_site) %></td>
    <td><%= link_to 'Destroy', user_site, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>

-->
